# created a tuple datatype
cardinal_numbers = ("first", "second", "third")

# Displayed the element at index 1
print(cardinal_numbers[1])

# unpacking the element inside our tuple into a new string
position1, position2, position3 = cardinal_numbers

# printing the values inside our new string
for positions in cardinal_numbers:
    print(positions)